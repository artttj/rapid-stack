---
description: Alias — learn any brand and generate a complete design system from it
---

Use the `hue` skill on the request below. The skill takes a URL, name, screenshot, or description, extracts the brand's design DNA (colors, typography, spacing, components, hero presets, icon kit), and generates a full design-system skill you can use in every session after that. Opinionated enough that two different sessions produce visually consistent output.

If the `hue` skill is not installed, tell the user to install it: `git clone https://github.com/dominikmartn/hue ~/.claude/skills/hue`.

$ARGUMENTS